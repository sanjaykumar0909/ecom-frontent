change Store's product browsing grid responsive
`product name` and `price` must be space-between

cart display
  each has basic product details and + - delete option too
  display total cost
  proceed to purchase

avatar button on navbar (login button)
